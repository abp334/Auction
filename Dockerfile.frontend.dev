FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

EXPOSE 8080

# Vite dev server; proxies /api to server service via VITE_SERVER_URL
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "8080"]
